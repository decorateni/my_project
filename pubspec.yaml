name: my_project
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
  # Для перевірки інтернет-з'єднання
  connectivity_plus: ^4.0.0
  # Для зберігання даних юзера
  shared_preferences: ^2.0.15
  # Для iOS стилів іконок
  cupertino_icons: ^1.0.8
  http: ^1.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
